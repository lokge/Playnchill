import React from 'react';

const Order = () => {
    return (
        <section className="order">
            <div className="container">
            </div>
        </section>
    );
};

export default Order;